/**
 * \file build_info.h
 *
 * \brief Build-time configuration info
 *
 *  Include platform header and pull in the auto-detected configuration.
 *  This file is normally generated at build time from config.h.
 */
#ifndef MBEDTLS_BUILD_INFO_H
#define MBEDTLS_BUILD_INFO_H

#include "mbedtls/config.h"

/* For V2.28 compatibility: build_info.h is a thin wrapper around config.h.
   In V3.0+, this file contains feature detection macros. */

#endif /* MBEDTLS_BUILD_INFO_H */
